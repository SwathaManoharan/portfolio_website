apiVersion: v1
kind: Secret
metadata:
  name: {{ include "devops-portfolio.fullname" . }}-secret
type: Opaque
stringData:
{{- range $key, $value := .Values.secret }}
  {{ $key }}: "{{ $value }}"
{{- end }}
